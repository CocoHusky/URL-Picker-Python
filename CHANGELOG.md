# Changelog

## v1.0.0

- Packaged as macOS `.app` bundle
- Added JSON summary section with browser counts, tab counts per browser, total URLs, and timestamp
- Changed window labels from `a`, `b`, `c` to `window_1`, `window_2`, `window_3`
- Updated output location to `~/Documents/Browser URL Grabber/json output/`
- Improved print output to show save path

## v0.1.0

- Initial project setup
- Added macOS browser tab collection script
- Added JSON-only export
- Added browser/window/tab organization
- Added Safari-specific support using `name` instead of `title`
- Added Chromium browser support using `title`
